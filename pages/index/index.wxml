<!--index.wxml-->
<!--<view>
  <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo"
    bindgetuserinfo="getUserInfo"> 登录 </button>
    <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view>
  <view>{{changMotto}}</view>
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view>
</view>
<button bindtap = "changMotto" class="button-hover">欢迎来到狗狗之家</button>
<button bindtap = "show" class="button-hover">给你的爱犬一个温暖的家</button>
<button type="default" size="{{defaultSize}}" loading="{{loading}}">default</button>
<view>{{show}}</view>-->
<view class="section">
<input type="number" placeholder="用户名" maxlength="11" />
</view>
<view class="section">
<input type="number" placeholder="密码" minlength="8" confirm-type="go" />
</view>
<view class="btn-area">
<navigator ur1="../iii/hello?title=hello" open-type="hello" hover-class="other-navigator-hocer">
<button type="primary" size="{{primarySize}}" loading="{{loading}}">登录</button></navigator>
</view>
<view bindtap="toast">
</view>
<view bindtap="toast">
</view>



